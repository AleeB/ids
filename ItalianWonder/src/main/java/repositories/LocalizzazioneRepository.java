package repositories;

import classes.Localizzazione;
import org.springframework.data.jpa.repository.JpaRepository;

public interface LocalizzazioneRepository extends JpaRepository<Localizzazione, Long> {
}
